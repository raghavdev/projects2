<?php

//allow editing of the staging data via the admin,
//Will require that the mapping system stop using schema? since that will not be accurate anymore
//would need to keep some record of the tables fields, or use a real show tables query
function bluemarble_staging_admin() {
  echo "todo";
}
